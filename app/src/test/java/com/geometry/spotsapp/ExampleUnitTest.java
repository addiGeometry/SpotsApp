package com.geometry.spotsapp;

import org.junit.Test;
import org.mockito.Mockito;
import org.osmdroid.views.MapView;

import static org.junit.Assert.*;

import com.geometry.spotsapp.model.Datastorage;
import com.geometry.spotsapp.model.DatastorageAccessFactory;
import com.geometry.spotsapp.model.DatastorageListAccess;

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */
public class ExampleUnitTest {
    @Test
    public void addition_isCorrect() {
        MapView mockView = Mockito.mock(MapView.class);
    }
}